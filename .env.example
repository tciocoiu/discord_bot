DISCORD_TOKEN=your_bot_token_here

# --- Railway (required in production) ---
# Add PostgreSQL in Railway, then reference its URL on the bot service:
# DATABASE_URL=${{Postgres.DATABASE_URL}}

# --- Local dev only (optional) ---
# DATABASE_URL=sqlite+aiosqlite:///data/loot.db

BASE_WEIGHT=1.0
BAD_LUCK_WEIGHT=0.5
ACTIVITY_WEIGHT=0.1
